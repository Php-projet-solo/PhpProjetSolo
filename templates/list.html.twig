<!DOCTYPE html>
<html>
<head>
    <title>Liste des compétitions</title>
</head>
<body>
<h1>Liste des compétitions</h1>
<a href="index.php?page=create">Créer une nouvelle compétition</a>
<table border="1">
    <thead>
    <tr>
        <th>ID</th>
        <th>Nom</th>
        <th>Description</th>
        <th>Date</th>
        <th>Actions</th>
    </tr>
    </thead>
    <tbody>
    {% for competition in competitions %}
        <tr>
            <td>{{ competition.idCompetition }}</td>
            <td>{{ competition.nom }}</td>
            <td>{{ competition.description }}</td>
            <td>{{ competition.date }}</td>
            <td>
                <a href="index.php?page=edit&id={{ competition.idCompetition }}">Modifier</a>
                <a href="index.php?page=delete&id={{ competition.idCompetition }}"
                   onclick="return confirm('Êtes-vous sûr ?')">Supprimer</a>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>
</body>
</html>
